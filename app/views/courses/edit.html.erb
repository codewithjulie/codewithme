<% provide(:title, "Edit Course") %>

<div class="container my-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <%= link_to "Back to course: #{@course.title}", course_path(@course), class: "btn btn-info mb-4"%>

      <h1>Edit Course</h1>
      <div>
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>
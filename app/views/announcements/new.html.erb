<% provide(:title, "New Announcement") %>

<div class="container my-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <%= link_to 'Back to All Announcements', course_announcements_path(@course), class: "btn btn-info mb-4"%>

      <h1>Create a New Announcement</h1>
      <div>
        <%= render "form"%>
      </div>
    </div>
  </div>
</div>